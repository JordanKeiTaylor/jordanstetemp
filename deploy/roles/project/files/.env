# Sadly this is the only way I could find to keep this in one place and reuse it in docker-compose.yml (YAML merge/references could work but you can't combine sequences apparently)
FNANNY_COMMAND=--deployment=local --project=solutions --controller_grpc_host=slave1 --fabric_version=fake --assembly=local_assembly --refresh_token=some_refresh_token --auth_server_url=http://slave1:21000 --controller_grpc_port=22000 --thor_grpc_host=slave1 --thor_grpc_port=22000 --thor_grpc_use_tls=false --logsink_host=slave1 --logsink_port=22000 --fabric_nanny_enable_docker_networking